function divide(a, b){
  if (b !== 0){
    return a/b
  }
  return "Error, b can't be equal to 0!"
};

const square = (number) => number * number;

const add = (number1, number2) => number1 + number2;
